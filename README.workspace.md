# ON Point Production Workspace

`Production` is the integration branch.

- `frontend`: one Vite React app for the player, admin, auth, and profile UI
- `backend`: Express API workspace

Profile/auth designs from `developer-3-profile` were copied into `frontend/src/components`.
